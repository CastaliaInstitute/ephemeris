{
  "name": "@castalia/ephemeris",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Castalia Swiss Ephemeris browser demo (GitHub Pages)",
  "scripts": {
    "dev": "vite",
    "generate-stars": "node scripts/generate-stars-catalog.mjs",
    "generate-data": "node scripts/generate-ephem-data.mjs",
    "generate-human-design": "node scripts/generate-ephem-data.mjs --only-human-design",
    "generate": "npm run generate-stars && npm run generate-data",
    "test": "node --test scripts/test-swisseph-runtime.mjs",
    "build": "npm run generate && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "swisseph-wasm": "^0.0.5"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
