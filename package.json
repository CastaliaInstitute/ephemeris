{
  "name": "@castalia/ephemeris",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Castalia Swiss Ephemeris browser demo (GitHub Pages)",
  "scripts": {
    "dev": "vite",
    "generate-data": "node scripts/generate-ephem-data.mjs",
    "build": "npm run generate-data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "swisseph-wasm": "^0.0.5"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
